# seniorprojectwebsite

* ONCE ALL OF THIS IS DONE THE REQUIREMENTS HAVE BEEN MET *

Start the server with "node server.js"

Run the code with http://localhost:3000/

* STILL NEED TO WORK ON *

1. Working order confirmation using nodemailer ( already done, need to test to confirm )
2. Move the contents in data.js and store it in MongoDB (created loadData.js for that just need to put node loadData.js in the terminal)
3. Connect all the data into MongoDB in order to be able to...( need to add data from data.js to get it to work)
    - create and store product data for the number of inventory and stock numbers
    - store order history
    - login information stored
4. Make the checkout page a bit better

